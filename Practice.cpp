#include <iostream>
using namespace std;
int sortedRotated(int arr[], int low, int high , int key ){
    while (low<=high)
    {
        int mid = (low + high) / 2;

        if (low <= mid)
        {
            
        }
        
    }
    
}
int main()
{
    
    return 0;
}